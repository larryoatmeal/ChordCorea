package models.music

object Transposer{

  // def tranpose(interval: Interval, note: DisplayNoteWithOctave){
  //   // val absDistance = 

  // }



  // def tranpose(orig: DisplayNoteWithOctave, dest: DisplayNoteWithOctave, note: DisplayNoteWithOctave){//transpose note such that it is relative to dest as it was relative to orig
  //   def interval = Interval.calculateInterval(orig, dest)

  //   val newLetter = Interval.numberLetterMap(Interval.letterNumberMap(note.displayNote.letter) + (interval.intervalClass - 1))


  // }


}